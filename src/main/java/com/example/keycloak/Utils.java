package com.example.keycloak;

public class Utils {

    public static String createJscId(String IdPId){

        //Time + IDP_ID has.
        //MD5 can we upgraded to sh256 or sh512 if ( need to check memory constrain )
        return "";
    }
}
